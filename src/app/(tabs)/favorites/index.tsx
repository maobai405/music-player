import { View, Text } from 'react-native';

const FavoritesScreen = () => {
  return (
    <View className="custom-container">
      <Text className="custom-text">FavoritesScreen PageIndex</Text>
    </View>
  );
};

export default FavoritesScreen;
