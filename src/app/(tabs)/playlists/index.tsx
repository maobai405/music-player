import { View, Text } from 'react-native';

const PlayListsScreen = () => {
  return (
    <View className="custom-container">
      <Text className="custom-text">PlayListsScreen PageIndex</Text>
    </View>
  );
};

export default PlayListsScreen;
